apiVersion: v1
kind: Pod
metadata:
  name: iperf3-client
  namespace: boutique
spec:
  containers:
  - name: iperf3-client
    image: networkstatic/iperf3
    command: ["sh", "-c", "sleep 3600"]
  restartPolicy: Never
